mvn clean package
java -jar target/money-manager-api-0.0.1-SNAPSHOT.jar
```

The application exposes a sample REST endpoint at /api/transactions.
Money Manager API

Minimal Spring Boot starter project for the Money Manager application.

Quick start

Prerequisites: Java 11+ and Maven installed.

Build and run:

```bash

